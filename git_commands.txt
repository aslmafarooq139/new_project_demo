touch <file name>-- to create new file from git 
 git add <file name ---to add the particular file into the spaging area
git status - for getting the status if the files being comiited or chnage iin the folder
git commit -m "message to be printed"-- for thecomming i.e. saving of thefile using command


any change that will be done to the files will eb considered as the modified to make them save need to commit them using the git



